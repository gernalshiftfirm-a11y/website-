{
  "name": "spice-garden-pos",
  "version": "1.0.0",
  "description": "Spice Garden — a single-page restaurant Point of Sale system with menu, orders, billing and earnings dashboard. Pure HTML/CSS/JS, localStorage-backed. Deployed via GitHub Pages.",
  "private": true,
  "scripts": {
    "serve": "npx --yes http-server -p 8080 -c-1 ."
  },
  "engines": {
    "node": ">=18"
  }
}
